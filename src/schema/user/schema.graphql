extend type Query {
  user(id: ID!): User
  users: UsersResponse
}

type User {
  id: ID!
  name: String!
  contact: String!
}

type UsersResponse {
  result: [User!]
}

extend type User {
  productSold: [Product!]
  productPurchased: [Product!]
}
