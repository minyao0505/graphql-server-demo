extend type Query {
  user(id: ID!): User
  users: [User!]
}

type User {
  id: ID!
  name: String!
  contact: String!
}

extend type User {
  productSold: [Product!]
  productPurchased: [Product!]
}
