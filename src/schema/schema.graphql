### User ###

# Demo
type User {
  id: ID!
  name: String!
  # TODO - Exercise 1 - Add a contact field for the user
}

# Demo
input CreateUserInput {}

extend type User {
  productSold: [Product!]
  productPurchased: [Product!]
}

### Product ###
enum ProductStatus {
  SOLD
  AVAILABLE
}

# TODO - Exercise 2 - create a product type that consists of id, name, price
# TODO - Exercise 4 - add a buyer field
type Product {}

# TODO - Exercise 3 - create a mutation for creating new product
input CreateProductInput {}

### Transaction ###
type Transaction {
  id: ID!
  product: Product!
  buyer: User!
  seller: User
}

input CreateTransactionInput {
  productId: ID!
  buyerId: ID!
}

### Query ###
type Query {
  user(id: ID!): User
  # Demo: users
  # TODO: Exercise 2: create a getProducts query that return a list of products
  # TODO: Extra exercise - create a getProduct query that take id as argument and return the product
}

### Mutation ###
type Mutation {
  # Demo: createUser
  # TODO - Exercise 3 - create a createProduct mutation that return the created product
    createTransaction(input: CreateTransactionInput!): Transaction!
}
