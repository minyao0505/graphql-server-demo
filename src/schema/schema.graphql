### User ###

# Demo
type User {
  id: ID!
  name: String!
  # Exercise 1
}

# Demo
input CreateUserInput {
 
}

extend type User {
  productSold: [Product!]
  productPurchased: [Product!]
}

### Product ###
enum ProductStatus {
  SOLD
  AVAILABLE
}

# Exercise 2
type Product {

}

# Exercise 3
input CreateProductInput {

}

### Transaction ###
type Transaction {
  id: ID!
  product: Product!
  buyer: User!
  seller: User
}

input CreateTransactionInput {
  productId: ID!
  buyerId: ID!
}

### Query ###
type Query {
  user(id: ID!): User
  # Demo: users
  # Exercise 2: getProducts
  # Extra exercise: getProduct
}

### Mutation ###
type Mutation {
  # Demo: createUser
  # Exercise 3: createProduct
    createTransaction(input: CreateTransactionInput!): Transaction!
}
